# Vacation Rate Project

## Prerequisites

For having all the Python Packages, please run the following command while being in the directory which contains this Readme file:

`pip install -r requirements.txt`

